{{#foreach posts}}
<section id="{{id}}" class="{{post_class}}">
	<h3 class="post-title">
	  <a href="{{url}}">{{title}}</a>
	  <span class="published">{{date published_at format='MMMM D, YYYY'}}</span>
	  {{#if featured}}FEATURED{{/if}}
	</h3>
</section>
{{/foreach}}